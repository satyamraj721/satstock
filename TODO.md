# TODO: Fix Invalid Href Attributes in React Components

## Steps to Complete:
- [x] Edit Hero.js: Replace all <a href="#"> tags with <button> elements styled like links
- [x] Edit CreateTicket.js: Replace <a href="#" onClick={(e) => e.preventDefault()}> tags with <button> elements styled like links, removing onClick
- [ ] Verify changes and ensure build passes
